@model List<OnlineShop.Models.EF.ProductCategory>
<ul>
    <h3>Danh mục sản phẩm</h3>
    @for (int i = 0; i < Model.Count; i++)
    {
        <li>
            <a title="" href="/san-pham/@Model[i].MetaTitle-@Model[i].ID">@Model[i].Name</a>
        </li>
    }
</ul>

